# roadmap: rhachet-brains-chutes

> checklist-style execution plan with ordered dependencies, acceptance criteria, and verification at each step

ref: [blueprint](./3.3.blueprint.v1.i1.md)

---

## phase 1: model catalog and config

> rewrite `BrainAtom.config.ts` with 11 chutes models and verified rates

### prereqs

- [ ] read [access research](./3.1.research.access._.v1.i1.md) — model ids, rates, context lengths
- [ ] read [claims research](./3.1.research.claims._.v1.i1.md) — rate verification, model availability
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 2 (model catalog) and section 4.1 (config codepath)

### checklist

- [ ] rename `TogetherBrainAtomSlug` to `ChutesBrainAtomSlug`
- [ ] declare 11 slug literals (8 carried + 3 chutes-exclusive)
- [ ] drop `together/llama4/maverick` and `together/llama3.3/70b` slugs
- [ ] add `chutes/deepseek/v3.2`, `chutes/devstral/2-123b`, `chutes/hermes/4-405b` slugs
- [ ] rewrite `CONFIG_BY_ATOM_SLUG` with all 11 model configs
- [ ] update each model id to match chutes (e.g., `Qwen/Qwen3-235B-A22B-Instruct` without `-tput`)
- [ ] update each `spec.cost.cash` to chutes rates (per access research section 3)
- [ ] set `spec.cost.cash.cache` to `$0` for both get and set (no cache rate on chutes)
- [ ] retain all `spec.gain` values for carried models; set new models to available data or `null`

### acceptance criteria

- [ ] `ChutesBrainAtomSlug` type has exactly 11 members
- [ ] no slug contains the prefix `together/`
- [ ] all 11 entries in `CONFIG_BY_ATOM_SLUG` have valid `model`, `description`, and `spec`
- [ ] all rates match the values in [access research](./3.1.research.access._.v1.i1.md) section 7.1

### verification

```sh
npm run test:types
```

---

## phase 2: factory function

> update `genBrainAtom.ts` to point at chutes api

### prereqs

- [ ] phase 1 complete
- [ ] read [access research](./3.1.research.access._.v1.i1.md) — section 1 (base url, auth)
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 4.2 (genBrainAtom codepath)

### checklist

- [ ] update import: `ChutesBrainAtomSlug` (was `TogetherBrainAtomSlug`)
- [ ] update `baseURL` to `'https://llm.chutes.ai/v1/'`
- [ ] update `apiKey` to `process.env.CHUTES_API_KEY`
- [ ] update `repo` to `'chutes'`
- [ ] retain the entire `ask` implementation (message composition, api call, parse, validate, metrics, episode)

### acceptance criteria

- [ ] `genBrainAtom` accepts `ChutesBrainAtomSlug` input
- [ ] openai client uses chutes base url and api key
- [ ] returned `BrainAtom` has `repo: 'chutes'`
- [ ] no reference to `together` remains in this file

### verification

```sh
npm run test:types
```

---

## phase 3: sdk contract

> rename exports in `contract/sdk/index.ts`

### prereqs

- [ ] phase 2 complete
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 4.3 (sdk codepath)

### checklist

- [ ] rename `getBrainAtomsByTogetherAI` to `getBrainAtomsByChutes`
- [ ] update type import: `ChutesBrainAtomSlug`
- [ ] export `ChutesBrainAtomSlug` type
- [ ] retain `genBrainAtom` re-export
- [ ] verify `src/index.ts` needs no changes (it re-exports `contract/sdk`)

### acceptance criteria

- [ ] `getBrainAtomsByChutes` is the sole catalog export
- [ ] `ChutesBrainAtomSlug` type is exported
- [ ] no reference to `together` remains in sdk files

### verification

```sh
npm run test:types
```

---

## phase 4: unit tests

> update unit test suite to match new contract

### prereqs

- [ ] phase 3 complete
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 5.1 (unit test codepath)

### checklist

- [ ] rename `describe('getBrainAtomsByTogetherAI')` to `describe('getBrainAtomsByChutes')`
- [ ] update expected atom count from 10 to 11
- [ ] update expected slug list to 11 chutes slugs
- [ ] update `repo` assertion to `'chutes'`
- [ ] update slug assertion in `genBrainAtom` test to a chutes slug

### acceptance criteria

- [ ] all unit tests pass
- [ ] test output shows 11 atoms
- [ ] no test references `together`

### verification

```sh
npm run test:unit
```

---

## phase 5: integration tests

> update integration test suite to call chutes api

### prereqs

- [ ] phase 4 complete (unit tests green)
- [ ] `CHUTES_API_KEY` env var set (via `source .agent/repo=.this/role=any/skills/use.apikeys.sh`)
- [ ] read [access research](./3.1.research.access._.v1.i1.md) — section 4 (error modes)
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 5.2 (integration test codepath)

### checklist

- [ ] update env var guard from `TOGETHER_API_KEY` to `CHUTES_API_KEY`
- [ ] update case1 slug to `chutes/qwen3/coder-next`
- [ ] update case1 repo assertion to `'chutes'`
- [ ] update case2 slug to `chutes/qwen3/coder-next`
- [ ] update case3 slug to `chutes/deepseek/v3.1` (or v3.2)
- [ ] update case4 to iterate over all 11 slugs
- [ ] retain `then.repeatably({ attempts: 3, criteria: 'SOME' })` for multi-model test (503 tolerance)
- [ ] retain secret-code brief test asset

### acceptance criteria

- [ ] all integration tests pass against live chutes api
- [ ] case2: structured output returns typed, validated result
- [ ] case3: episode continuation works (second response references first)
- [ ] case4: all 11 models return valid responses (with retry tolerance)

### verification

```sh
source .agent/repo=.this/role=any/skills/use.apikeys.sh
npm run test:integration
```

---

## phase 6: acceptance tests

> create blackbox acceptance test suite

### prereqs

- [ ] phase 5 complete (integration tests green)
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 5.3 (acceptance test plan)

### checklist

- [ ] create `src/contract/sdk/index.acceptance.test.ts`
- [ ] case1: verify `getBrainAtomsByChutes` and `genBrainAtom` are exported
- [ ] case2: verify `getBrainAtomsByChutes()` returns 11 `BrainAtom` instances with valid `BrainSpec`
- [ ] case3: verify a brain atom can be asked a question and returns typed output with cost metrics

### acceptance criteria

- [ ] all acceptance tests pass
- [ ] tests use only the public sdk contract (no internal imports)
- [ ] test output confirms 11 atoms with valid specs

### verification

```sh
source .agent/repo=.this/role=any/skills/use.apikeys.sh
npm run test:acceptance
```

---

## phase 7: package metadata

> update package.json for chutes identity

### prereqs

- [ ] phase 6 complete (all tests green)
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 6.1

### checklist

- [ ] update `name` to `rhachet-brains-chutes`
- [ ] update `description` to reference chutes
- [ ] update `repository.url` to `rhachet-brains-chutes`
- [ ] retain all dependencies (no version changes)
- [ ] retain version at `0.1.0` (or as directed)

### acceptance criteria

- [ ] `npm run build` succeeds
- [ ] package name is `rhachet-brains-chutes`

### verification

```sh
npm run build
```

---

## phase 8: readme

> full rewrite of readme for chutes

### prereqs

- [ ] phase 7 complete
- [ ] read [vision](./1.vision.md) — usecase examples, mental model, evaluation
- [ ] read [access research](./3.1.research.access._.v1.i1.md) — base url, auth, model catalog
- [ ] read [claims research](./3.1.research.claims._.v1.i1.md) — cost comparison, reliability, privacy

### checklist

- [ ] update title to `rhachet-brains-chutes`
- [ ] update model table: 11 chutes models with rates
- [ ] update usage examples with chutes slugs and `CHUTES_API_KEY`
- [ ] add "why chutes" section: cost comparison vs together ai
- [ ] add reliability note: 503 risk, miner-dependent availability, no enterprise sla
- [ ] add privacy note: prompt visibility to miners unless tee-enabled
- [ ] update install and env var instructions

### acceptance criteria

- [ ] readme references only chutes (no `together` mentions except in comparison)
- [ ] model table has 11 entries
- [ ] usage examples compile conceptually (correct slugs, env var, imports)

### verification

visual review of rendered markdown

---

## phase 9: brief update

> update brain-config-pattern brief to use chutes examples

### prereqs

- [ ] phase 8 complete
- [ ] read [blueprint](./3.3.blueprint.v1.i1.md) — section 6.3

### checklist

- [ ] replace all `together/` slug examples with `chutes/` equivalents
- [ ] rename `TogetherBrainAtomSlug` to `ChutesBrainAtomSlug`
- [ ] rename `TogetherBrainReplSlug` to `ChutesBrainReplSlug`
- [ ] update config examples to show chutes model entries

### acceptance criteria

- [ ] no `together` references remain in the brief
- [ ] examples match the actual `BrainAtom.config.ts` structure

### verification

visual review

---

## phase 10: full test suite

> run all tests end-to-end to confirm green

### prereqs

- [ ] all phases 1-9 complete
- [ ] `CHUTES_API_KEY` env var set

### checklist

- [ ] run type checks
- [ ] run unit tests
- [ ] run integration tests
- [ ] run acceptance tests
- [ ] run build

### acceptance criteria

- [ ] all test suites pass
- [ ] build succeeds
- [ ] no references to `together` remain in src/ (except comments that compare suppliers)

### verification

```sh
source .agent/repo=.this/role=any/skills/use.apikeys.sh
npm run test:types
npm run test:unit
npm run test:integration
npm run test:acceptance
npm run build
```

---

## dependency graph

```
phase 1 (config)
  └── phase 2 (factory)
       └── phase 3 (sdk)
            └── phase 4 (unit tests)
                 └── phase 5 (integration tests)
                      └── phase 6 (acceptance tests)
                           └── phase 7 (package.json)
                                └── phase 8 (readme)
                                     └── phase 9 (brief)
                                          └── phase 10 (full suite)
```

all phases are sequential — each depends on the prior phase. no parallel execution paths.
