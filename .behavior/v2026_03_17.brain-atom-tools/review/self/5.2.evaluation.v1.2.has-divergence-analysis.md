# has-divergence-analysis

## review date
2026-03-17

## methodology

compared each section of blueprint (3.3.1.blueprint.product.v1.i1.md) vs implementation with skeptical lens. for each claim, verified against actual source files and documented evidence.

---

## section-by-section comparison

### summary comparison

**blueprint claims:**
1. cast functions — convert between rhachet and openai tool formats
2. ask signature extension — add TPlugs generic and tool-aware prompt type
3. tool call extraction — detect and convert tool_calls from api response
4. tool continuation — support prompt as tool execution results

**verification:**

| claim | evidence | why it holds |
|-------|----------|--------------|
| cast functions | 3 files created: castIntoChutesToolDef.ts (18 lines), castFromChutesToolCall.ts (14 lines), castIntoChutesToolMessages.ts (37 lines) | each file performs exactly one conversion as documented; file names match rhachet cast convention (`castInto*` for target, `castFrom*` for source) |
| ask signature | genBrainAtom.ts line 58: `<TPlugs extends BrainAtomPlugs>` | generic parameter added at exact location documented; enables typed tool support |
| tool call extraction | genBrainAtom.ts lines 152-155, 231-242 | checks `tool_calls` on response, maps via castFromChutesToolCall |
| tool continuation | genBrainAtom.ts lines 97-107 | dispatches on prompt type: string vs BrainPlugToolExecution[] |

**verdict**: summary matches. all 4 components present with correct implementation.

---

### filediff comparison

| blueprint file | implementation | why it holds |
|----------------|----------------|--------------|
| `[+] castIntoChutesToolDef.ts` | created, 18 lines | file exists at `src/infra/cast/castIntoChutesToolDef.ts`; exports single function; 18 lines including imports and docstring |
| `[+] castFromChutesToolCall.ts` | created, 14 lines | file exists at `src/infra/cast/castFromChutesToolCall.ts`; exports single function; 14 lines total |
| `[+] castIntoChutesToolMessages.ts` | created, 37 lines | file exists at `src/infra/cast/castIntoChutesToolMessages.ts`; exports single function; 37 lines total |
| `[~] genBrainAtom.ts` | modified | tool use imports added (lines 18-20), TPlugs generic added (line 58), tools parameter added (lines 117-126), tool_calls extraction added (lines 231-242) |
| `[~] genBrainAtom.integration.test.ts` | modified | 4 new test cases: [case5] lines 184-238, [case6] lines 241-303, [case8] lines 305-338, [case9] lines 341-399 |

**verdict**: filediff matches. all 5 file changes documented, all verified in git diff.

---

### codepath comparison

#### castIntoChutesToolDef.ts — verified line-by-line

**blueprint codepath:**
```
castIntoChutesToolDef
  ├── [+] input: { tool: BrainPlugToolDefinition }
  ├── [+] output: OpenAI.ChatCompletionTool
  └── [+] conversion logic
        ├── type: 'function'
        ├── function.name ← tool.slug
        ├── function.description ← tool.description
        └── function.parameters ← z.toJSONSchema(tool.schema.input)
```

**actual code (lines 9-18):**
```ts
export const castIntoChutesToolDef = (input: {
  tool: BrainPlugToolDefinition;
}): OpenAI.ChatCompletionTool => ({
  type: 'function' as const,
  function: {
    name: input.tool.slug,
    description: input.tool.description,
    parameters: z.toJSONSchema(input.tool.schema.input),
  },
});
```

**why it holds:** every blueprint codepath matches exactly:
- input type: `{ tool: BrainPlugToolDefinition }` (line 9-10)
- output type: `OpenAI.ChatCompletionTool` (line 11)
- type: `'function' as const` (line 12)
- function.name: `input.tool.slug` (line 14)
- function.description: `input.tool.description` (line 15)
- function.parameters: `z.toJSONSchema(input.tool.schema.input)` (line 16)

---

#### castFromChutesToolCall.ts — verified line-by-line

**blueprint codepath:**
```
castFromChutesToolCall
  ├── [+] input: { call: OpenAI.ChatCompletionMessageToolCall }
  ├── [+] output: BrainPlugToolInvocation
  └── [+] conversion logic
        ├── exid ← call.id
        ├── slug ← call.function.name
        └── input ← JSON.parse(call.function.arguments)
```

**actual code (lines 8-14):**
```ts
export const castFromChutesToolCall = (input: {
  call: OpenAI.ChatCompletionMessageToolCall;
}): BrainPlugToolInvocation => ({
  exid: input.call.id,
  slug: input.call.function.name,
  input: JSON.parse(input.call.function.arguments),
});
```

**why it holds:** exact match:
- input type: `{ call: OpenAI.ChatCompletionMessageToolCall }` (lines 8-9)
- output type: `BrainPlugToolInvocation` (line 10)
- exid: `input.call.id` (line 11)
- slug: `input.call.function.name` (line 12)
- input: `JSON.parse(input.call.function.arguments)` (line 13)

---

#### castIntoChutesToolMessages.ts — verified line-by-line

**blueprint codepath:**
```
castIntoChutesToolMessages
  ├── [+] input: { executions: BrainPlugToolExecution[] }
  ├── [+] output: OpenAI.ChatCompletionMessageParam[]
  └── [+] conversion logic
        ├── [+] assistant message with tool_calls array
        │     └── map executions to { id, type, function: { name, arguments } }
        └── [+] tool result messages
              └── for each execution: { role: 'tool', tool_call_id, content }
```

**actual code:**

assistant message (lines 14-25):
```ts
messages.push({
  role: 'assistant' as const,
  content: null,
  tool_calls: input.executions.map((exec) => ({
    id: exec.exid,
    type: 'function' as const,
    function: {
      name: exec.slug,
      arguments: JSON.stringify(exec.input),
    },
  })),
});
```

tool result messages (lines 28-34):
```ts
for (const execution of input.executions) {
  messages.push({
    role: 'tool' as const,
    tool_call_id: execution.exid,
    content: JSON.stringify(execution.output),
  });
}
```

**why it holds:** exact match:
- input type: `{ executions: BrainPlugToolExecution[] }` (lines 8-9)
- output type: `OpenAI.ChatCompletionMessageParam[]` (line 10)
- assistant message with tool_calls array: lines 14-25
- tool result messages: lines 28-34

---

#### genBrainAtom.ts — verified key codepaths

| blueprint codepath | actual line | evidence |
|--------------------|-------------|----------|
| `TPlugs generic parameter` | line 58 | `<TPlugs extends BrainAtomPlugs = BrainAtomPlugs>` |
| `prompt: string \| BrainPlugToolExecution[]` | line 63 | `prompt: string \| BrainPlugToolExecution[]` |
| `return: BrainOutput<TOutput, 'atom', TPlugs>` | line 67 | `): Promise<BrainOutput<TOutput, 'atom', TPlugs>>` |
| `BrainPlugToolExecution[] → castIntoChutesToolMessages()` | lines 103-105 | `if (Array.isArray(input.prompt)) { promptMessages = castIntoChutesToolMessages({ executions: input.prompt }); }` |
| `tools parameter` | lines 117-126 | `const toolsParam = input.plugs?.tools?.length ? { tools: input.plugs.tools.map(castIntoChutesToolDef), tool_choice: 'auto' as const } : {};` |
| `check for tool_calls` | lines 152-155 | `const hasToolCalls = firstChoice.message.tool_calls && firstChoice.message.tool_calls.length > 0;` |
| `extract via castFromChutesToolCall()` | lines 231-242 | `calls: hasToolCalls ? { tools: (firstChoice.message.tool_calls ?? []).map((call) => castFromChutesToolCall({ call })) } : null` |
| `output null when tools called` | line 236 | `output: hasToolCalls ? null : outputParsed` |

**why it holds:** all codepaths verified against actual source lines. each transformation documented in blueprint exists in implementation.

**additional codepath not in blueprint:**
- `tool_choice: 'auto'` (line 124) — this was implicit in "tools parameter" but explicit in implementation. this is not a divergence — tool_choice is required for correct api behavior with tools.

---

### test coverage comparison

| blueprint test | implementation | match? | evidence |
|----------------|----------------|--------|----------|
| [case5] tool invocation | lines 184-238 | ✓ | test named `[case5] tool invocation`, asserts output null, calls.tools defined, slug matches, input typed |
| [case6] tool continuation | lines 241-303 | ✓ | test named `[case6] tool continuation`, asserts first result has calls, second result has output with '56' |
| [case7] direct answer with tools | **omitted** | ✗ divergence | no test with this name; api limitation prevents reliable verification (see divergence section) |
| [case8] metrics with tools | lines 305-338 | ✓ | test named `[case8] metrics with tools`, asserts metrics defined, time cost present, token counts > 0 |
| [case9] tool error signal | lines 341-399 | ✓ | test named `[case9] tool error signal`, asserts error fed back, brain continues gracefully |

**verdict**: 4 of 5 tests implemented. [case7] omitted with documented rationale.

---

## divergences found

| section | divergence | severity | disposition |
|---------|-----------|----------|-------------|
| test coverage | [case7] direct answer with tools omitted | documented | backup |

---

## skeptical check: what would a hostile reviewer find?

### 1. [case7] omitted
**found**: already documented in divergence table.
**why not a problem**: backed up with api limitation rationale. code handles the case (lines 244-254); test would be flaky.

### 2. tool_choice: 'auto' not in blueprint
**found**: line 124 adds `tool_choice: 'auto'` which was not explicit in blueprint.
**why not a problem**: implicit in "tools parameter" concept. openai-compatible api requires tool_choice for function call behavior. minor implementation detail, not semantic divergence.

### 3. error signal type in tests
**found**: test uses `'error:constraint'` (line 376), some earlier docs mentioned `'error:unexpected'`.
**why not a problem**: both are valid error signals defined in rhachet. the test covers the error signal usecase regardless of which specific signal type. the behavior (brain receives error, adapts) is verified.

### 4. response_format conditional logic
**found**: implementation has `shouldUseResponseFormat = !hasTools || isToolContinuation` (line 130).
**why not a problem**: blueprint stated "response_format when no tools OR when in continuation mode". implementation matches exactly.

---

## conclusion

**one divergence found: [case7] test omitted.**

the divergence is documented and backed up with api limitation rationale. all other blueprint claims verified against actual source with line-by-line evidence. the implementation faithfully follows the blueprint.

