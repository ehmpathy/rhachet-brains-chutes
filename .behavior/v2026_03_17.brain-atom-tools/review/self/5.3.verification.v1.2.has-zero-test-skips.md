# has-zero-test-skips

## review date
2026-03-17

## methodology
searched all test files for forbidden patterns

---

## .skip() and .only() check

```sh
grep -r '\.(skip|only)\(' **/*.test.ts
```

**result**: no matches

---

## silent credential bypasses check

checked for patterns like:
- `if (!credentials) return`
- `if (!process.env.API_KEY) return`

**result**: none found

the integration tests use explicit credential check:
```ts
if (!process.env.CHUTES_API_KEY)
  throw new BadRequestError('CHUTES_API_KEY is required for integration tests');
```

this **throws** rather than silent skip — correct behavior.

---

## prior failures check

**result**: no prior failures carried forward

all 58 tests pass:
- unit: 6 passed
- integration: 45 passed
- acceptance: 7 passed

---

## fresh eyes review

re-verified each claim by re-ran the searches just now.

### verification 1: .skip/.only check

ran grep for `.skip` and `.only` patterns in test files:

```
grep -E '\.(skip|only)\(' **/*.test.ts
```

**result**: no matches found. confirmed zero forbidden patterns.

### verification 2: silent credential bypasses check

ran grep for silent return patterns:

```
grep -E 'if.*!.*API_KEY.*return' **/*.test.ts
```

**result**: no matches found.

then verified the credential pattern used:

```
grep 'CHUTES_API_KEY' **/*.test.ts
```

**found 3 test files with credential checks**:
- genBrainAtom.integration.test.ts:17-18 → throws BadRequestError
- getModelsAvailable.integration.test.ts:7-8 → throws BadRequestError
- index.acceptance.test.ts:8-9 → throws BadRequestError

all three throw errors on absent credentials rather than pass silently. this is the correct behavior.

### verification 3: test run count

test output shows:
- 58 tests executed
- 58 tests passed
- 0 tests skipped

### why it holds

1. no `.skip()` or `.only()` found — developers did not leave shortcuts
2. credential gates throw BadRequestError — tests fail loud, not silent
3. 58/58 tests pass — no prior failures carried forward

### issues found

none. all three verification steps confirm zero skips.

---

## conclusion

**zero skips verified.** no .skip(), no .only(), no silent bypasses, no prior failures.
